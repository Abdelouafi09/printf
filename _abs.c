#include "main.h"
/**
 * _abs - returns the absolute value of a digit
 * @n: digit to work on
 *
 * Return: return positive value of the digit
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}
